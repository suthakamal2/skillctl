<!-- MIT-licensed -->
# skillctl Claude Code Plugin

## Installation

Drop the `skillctl/` directory into `~/.claude/skills/` or your project's `.claude/skills/`.

## Verification

Run `/skills list` in Claude Code to verify it is loaded.
