---
id: secrets-management
title: "Secrets Management"
tier: 2
triggers: [secret, credentials, api key, vault, env var]
summary: "Secrets in a manager, never in code or logs; rotate; scope per service."
---

# Secrets Management

Secrets in a manager, never in code or logs; rotate; scope per service.

## When this applies
Loads when the prompt mentions: secret, credentials, api key, vault, env var.

## Guidance
- Secrets in a manager, never in code or logs; rotate; scope per service.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
