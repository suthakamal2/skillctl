---
id: kubernetes
title: "Kubernetes"
tier: 2
triggers: [kubernetes, k8s, pod, helm, kubectl]
deps: [docker]
summary: "Set resource requests/limits, liveness/readiness probes, no :latest tags."
---

# Kubernetes

Set resource requests/limits, liveness/readiness probes, no :latest tags.

## When this applies
Loads when the prompt mentions: kubernetes, k8s, pod, helm, kubectl.

## Guidance
- Set resource requests/limits, liveness/readiness probes, no :latest tags.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
