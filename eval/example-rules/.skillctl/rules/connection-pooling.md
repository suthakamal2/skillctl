---
id: connection-pooling
title: "Connection Pooling"
tier: 2
triggers: [connection pool, pool, max connections]
summary: "Bound the pool; set timeouts; one pool per service; watch for leaks."
---

# Connection Pooling

Bound the pool; set timeouts; one pool per service; watch for leaks.

## When this applies
Loads when the prompt mentions: connection pool, pool, max connections.

## Guidance
- Bound the pool; set timeouts; one pool per service; watch for leaks.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
