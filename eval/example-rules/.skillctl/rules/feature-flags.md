---
id: feature-flags
title: "Feature Flags"
tier: 2
triggers: [feature flag, flag, toggle, gradual rollout]
summary: "Gate risky changes behind a flag; default off; clean up stale flags."
---

# Feature Flags

Gate risky changes behind a flag; default off; clean up stale flags.

## When this applies
Loads when the prompt mentions: feature flag, flag, toggle, gradual rollout.

## Guidance
- Gate risky changes behind a flag; default off; clean up stale flags.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
