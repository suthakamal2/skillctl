---
id: caching
title: "Caching"
tier: 2
triggers: [cache, caching, ttl, invalidation]
summary: "Cache the expensive and stable; explicit TTL; name an invalidation strategy."
---

# Caching

Cache the expensive and stable; explicit TTL; name an invalidation strategy.

## When this applies
Loads when the prompt mentions: cache, caching, ttl, invalidation.

## Guidance
- Cache the expensive and stable; explicit TTL; name an invalidation strategy.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
