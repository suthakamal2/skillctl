---
id: authentication
title: "Authentication"
tier: 2
triggers: [authentication, login, auth, session]
deps: [jwt]
summary: "Standard flows only; secure session handling; MFA where it matters."
---

# Authentication

Standard flows only; secure session handling; MFA where it matters.

## When this applies
Loads when the prompt mentions: authentication, login, auth, session.

## Guidance
- Standard flows only; secure session handling; MFA where it matters.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
