---
id: error-handling
title: "Error Handling"
tier: 2
triggers: [error handling, exception, retry, timeout]
summary: "Fail loud at boundaries, retry transient with backoff, never swallow errors."
---

# Error Handling

Fail loud at boundaries, retry transient with backoff, never swallow errors.

## When this applies
Loads when the prompt mentions: error handling, exception, retry, timeout.

## Guidance
- Fail loud at boundaries, retry transient with backoff, never swallow errors.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
