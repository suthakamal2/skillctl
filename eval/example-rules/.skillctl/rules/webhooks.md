---
id: webhooks
title: "Webhooks"
tier: 2
triggers: [webhook, callback url, event delivery]
deps: [queue-processing]
summary: "Verify signatures, respond fast then process async, retry with backoff, dedupe."
---

# Webhooks

Verify signatures, respond fast then process async, retry with backoff, dedupe.

## When this applies
Loads when the prompt mentions: webhook, callback url, event delivery.

## Guidance
- Verify signatures, respond fast then process async, retry with backoff, dedupe.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
