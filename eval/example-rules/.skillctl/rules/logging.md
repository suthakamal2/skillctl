---
id: logging
title: "Logging"
tier: 2
triggers: [logging, log, structured logs]
summary: "Structured JSON logs, correlation IDs, no PII, levels used consistently."
---

# Logging

Structured JSON logs, correlation IDs, no PII, levels used consistently.

## When this applies
Loads when the prompt mentions: logging, log, structured logs.

## Guidance
- Structured JSON logs, correlation IDs, no PII, levels used consistently.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
