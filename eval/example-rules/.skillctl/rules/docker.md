---
id: docker
title: "Docker"
tier: 2
triggers: [docker, container, image, dockerfile]
summary: "Pin base images, multi-stage builds, non-root user, minimal layers."
---

# Docker

Pin base images, multi-stage builds, non-root user, minimal layers.

## When this applies
Loads when the prompt mentions: docker, container, image, dockerfile.

## Guidance
- Pin base images, multi-stage builds, non-root user, minimal layers.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
