---
id: rollback
title: "Rollback"
tier: 2
triggers: [rollback, revert, roll back, hotfix]
summary: "Prefer rollback over forward-fix under incident; keep the last good artifact."
---

# Rollback

Prefer rollback over forward-fix under incident; keep the last good artifact.

## When this applies
Loads when the prompt mentions: rollback, revert, roll back, hotfix.

## Guidance
- Prefer rollback over forward-fix under incident; keep the last good artifact.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
