---
id: cors
title: "Cors"
tier: 2
triggers: [cors, cross-origin, preflight]
summary: "Allow-list origins; never reflect arbitrary Origin; restrict methods/headers."
---

# Cors

Allow-list origins; never reflect arbitrary Origin; restrict methods/headers.

## When this applies
Loads when the prompt mentions: cors, cross-origin, preflight.

## Guidance
- Allow-list origins; never reflect arbitrary Origin; restrict methods/headers.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
