---
id: api-design
title: "Api Design"
tier: 2
triggers: [api design, endpoint, rest, api contract]
deps: [rest-conventions]
summary: "Design the contract first; version it; keep responses consistent and typed."
---

# Api Design

Design the contract first; version it; keep responses consistent and typed.

## When this applies
Loads when the prompt mentions: api design, endpoint, rest, api contract.

## Guidance
- Design the contract first; version it; keep responses consistent and typed.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
