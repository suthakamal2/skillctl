---
id: graphql
title: "Graphql"
tier: 2
triggers: [graphql, resolver, schema stitching]
summary: "Guard against deep/expensive queries; dataloader for N+1; persisted queries."
---

# Graphql

Guard against deep/expensive queries; dataloader for N+1; persisted queries.

## When this applies
Loads when the prompt mentions: graphql, resolver, schema stitching.

## Guidance
- Guard against deep/expensive queries; dataloader for N+1; persisted queries.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
