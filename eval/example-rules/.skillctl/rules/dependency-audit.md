---
id: dependency-audit
title: "Dependency Audit"
tier: 2
triggers: [dependency, vulnerability, cve, audit, supply chain]
summary: "Pin and lock deps; scan for CVEs in CI; review transitive additions."
---

# Dependency Audit

Pin and lock deps; scan for CVEs in CI; review transitive additions.

## When this applies
Loads when the prompt mentions: dependency, vulnerability, cve, audit, supply chain.

## Guidance
- Pin and lock deps; scan for CVEs in CI; review transitive additions.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
