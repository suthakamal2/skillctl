---
id: jwt
title: "Jwt"
tier: 2
triggers: [jwt, token, bearer, claims]
summary: "Short-lived access tokens, verify signature + audience + expiry, rotate keys."
---

# Jwt

Short-lived access tokens, verify signature + audience + expiry, rotate keys.

## When this applies
Loads when the prompt mentions: jwt, token, bearer, claims.

## Guidance
- Short-lived access tokens, verify signature + audience + expiry, rotate keys.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
