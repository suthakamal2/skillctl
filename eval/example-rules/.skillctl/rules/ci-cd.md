---
id: ci-cd
title: "Ci Cd"
tier: 2
triggers: [pipeline, continuous integration, github actions, build pipeline]
summary: "Fast feedback first; cache deps; fail the pipeline on lint, type, and test."
---

# Ci Cd

Fast feedback first; cache deps; fail the pipeline on lint, type, and test.

## When this applies
Loads when the prompt mentions: pipeline, continuous integration, github actions, build pipeline.

## Guidance
- Fast feedback first; cache deps; fail the pipeline on lint, type, and test.
- Make the safe choice the default; document the exception when you deviate.
- Prefer the boring, well-understood option over the clever one.
